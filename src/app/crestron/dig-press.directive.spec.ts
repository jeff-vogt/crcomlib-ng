import { DigPressDirective } from './dig-press.directive';

describe('DigPressDirective', () => {
  it('should create an instance', () => {
    const directive = new DigPressDirective();
    expect(directive).toBeTruthy();
  });
});
