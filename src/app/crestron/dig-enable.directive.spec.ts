import { DigEnableDirective } from './dig-enable.directive';

describe('DigEnableDirective', () => {
  it('should create an instance', () => {
    const directive = new DigEnableDirective();
    expect(directive).toBeTruthy();
  });
});
