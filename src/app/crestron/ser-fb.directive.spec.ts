import { SerFbDirective } from './ser-fb.directive';

describe('SerFbDirective', () => {
  it('should create an instance', () => {
    const directive = new SerFbDirective();
    expect(directive).toBeTruthy();
  });
});
