import { AnaSendDirective } from './ana-send.directive';

describe('AnaSendDirective', () => {
  it('should create an instance', () => {
    const directive = new AnaSendDirective();
    expect(directive).toBeTruthy();
  });
});
