import { DigVisibleDirective } from './dig-visible.directive';

describe('DigVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new DigVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
