import { DigFbDirective } from './dig-fb.directive';

describe('DigFbDirective', () => {
  it('should create an instance', () => {
    const directive = new DigFbDirective();
    expect(directive).toBeTruthy();
  });
});
