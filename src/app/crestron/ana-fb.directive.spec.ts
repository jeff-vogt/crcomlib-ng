import { AnaFbDirective } from './ana-fb.directive';

describe('AnaFbDirective', () => {
  it('should create an instance', () => {
    const directive = new AnaFbDirective();
    expect(directive).toBeTruthy();
  });
});
